echo -e "coucou\n\t" c'estmoua | echo $1 $2
ifconfig | grep 192
ifconfig | grep UP
ls | wc -l
cat contacts.txt | sort
cat contacts.txt | sort -r
ls | echo $2
echo -e "coucou\n\t" c'estmoua|echo $1 $2
ifconfig|grep 192
ifconfig|grep UP
ls|wc -l
cat Makfile| sort
cat Makfile| sort -r
ls |echo $2
